streamlit==1.41.1
duckdb==1.3.1
plotly==5.24.1
pandas==2.2.3
# Constrain numpy to the 2.0–2.4 range. pyarrow 25's wheels are built for numpy
# 2.x (so numpy 1.x segfaults them), but the newest numpy 2.5.x is ABI-broken
# against the pinned duckdb 1.3.1 wheel and segfaults the app on startup on
# Streamlit Community Cloud ("Error running app" / "Segmentation fault" in logs).
# 2.0–2.4 is the compatible window.
numpy>=2.0,<2.5
