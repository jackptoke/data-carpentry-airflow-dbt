# Extra packages installed on top of the apache/airflow base image, into the
# Airflow environment. dbt itself runs from the separate project virtualenv
# (.venv) that Cosmos points at.

# Orchestration + transformation
astronomer-cosmos==1.10.1
# dbt-core is pulled in transitively by dbt-duckdb; the standalone `dbt`
# meta-package is deprecated and intentionally not installed here.
dbt-duckdb==1.9.4
duckdb==1.3.1

# Used by the ingestion asset
pandas==2.2.3
